@model IEnumerable<Role>
<head>
    <title>
        Index of roles
       
    </title>
</head>
@if (this.User.IsInRole("Admin"))
{
    <p> <a asp-action="Create">Create New</a> </p>
}
@TempData["User Not Found"]
@* <form method="post" asp-action="Index" asp-controller="Role">
    <div class="input-group">
        <div class="form-outline">
            <input type="text" class="form-control" name="Email" /> 
            <label class="form-label">Search</label>
        </div>
        <button type="submit" class="btn btn-primary">
            <i class="fas fa-search"></i>
        </button>
    </div>
</form> *@




@* sgsfstarts here *@
<form class="form-inline" method="post" asp-action="Index" asp-controller="Role">
    <input class="form-control mr-sm-2" type="text" placeholder="Search" name="Email" aria-label="Search">
    <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
</form>

@* starts here *@
<table class="table">
   
<thead>
    
        <tr>
            <th>
                Email
            </th>
            <th>
                Roll Name
            </th>
         </tr>


    
</thead>
<tbody>
    @foreach(var item in Model)
        {
            <tr>
                <td>
                    @item.Email
                </td>
                <td>
                    @item.RollName
                </td>
            </tr>
        }
    </tbody>


</table>